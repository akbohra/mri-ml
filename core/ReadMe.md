# Intro to Core

- To create Framework-agnostic backbone for the repo
- base.py : to create abstract base classess for model, dataset and tasks 
- config.py: for config loading/validation; how do we parametrize runs. 
- engine.py: for training / validation loop; how do we train repeatedly.